## Setup Summary
This project was created on a Linux system (Fedora/RHEL).

Main commands used:
- useradd, groupadd
- chmod, chown
- systemctl
- firewall-cmd
- lsblk, mount, LVM
- journalctl
- bash & cron